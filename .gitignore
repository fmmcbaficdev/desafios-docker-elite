# ── Documentos (mantidos apenas localmente) ───────────────────────
*.md
!README.md

# ── SO / Editores ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# ── Docker / logs locais ──────────────────────────────────────────
*.log

# ── Segredos / variaveis de ambiente ──────────────────────────────
# NUNCA commitar arquivos .env com credenciais reais.
# Use .env.example como template versionado.
.env
.env.*
!.env.example
!.env.*.example

# ── Repositorios externos clonados para desenvolvimento ───────────
# Apps de terceiros usadas nos desafios (com .git proprio) que ficam
# na raiz para conveniencia mas nao pertencem a este repositorio.
/kube-news/
